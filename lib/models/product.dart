class Product {
  int key;
  int storeKey;
  String name;
  String description;
  String photo;
  double price;
  int quantity;

  Product({this.key, this.storeKey, this.name, this.description, this.photo, this.price, this.quantity});
}