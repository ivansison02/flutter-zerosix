class Product {
  int key;
  String name;
  String description;
  String photo;
  double price;
  int quantity;

  Product({this.key, this.name, this.description, this.photo, this.price, this.quantity});
}